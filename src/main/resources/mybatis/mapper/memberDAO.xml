<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.pilotproject.dao.memberDAO">
	<select id="memberList" resultType="com.springboot.pilotproject.dto.memberDTO">
		select * from member order by mno desc
	</select>
	<select id="memberView" resultType="com.springboot.pilotproject.dto.memberDTO">
		select * from member where mno = #{mno}
	</select>
	<insert id="memberWrite">
		insert into member(mno, mid, mpasswd, mname, mtel, mgender, maddr, mzipno, memail, mdate, mauthority)
		 values(member_seq.NEXTVAL, #{mid}, #{mpasswd}, #{mname}, #{mtel}, #{mgender}, #{maddr}, #{mzipno}, #{memail}, SYSDATE, 'USER')
	</insert>
	<update id="memberUpdate">
		update member
		 set mpasswd = #{mpasswd}, mtel = #{mtel}, maddr = #{maddr}, mzipno = #{mzipno}, memail = #{memail}
		 where mid = #{mid}
	</update>
	<delete id="memberDelete">
		delete from member
		 where mid = #{mid}
	</delete>
	<select id="findById" resultType="com.springboot.pilotproject.dto.memberDTO">
		select * from member where mid = #{mid}
	</select>
</mapper>
